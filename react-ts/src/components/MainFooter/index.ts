/**
 * MainFooter 컴포넌트 배럴 익스포트
 * 다른 파일에서 import 시 경로를 간소화하기 위함
 * 
 * 사용법:
 * import MainFooter from 'components/MainFooter';
 * (MainFooter/MainFooter 대신 MainFooter만 써도 됨)
 */
export { default } from './MainFooter';
